{% extends 'base.html.twig' %}

{% block title %}BreakdownSymptom index{% endblock %}

{% block body %}
    <h1>BreakdownSymptom index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for breakdown_symptom in breakdown_symptoms %}
            <tr>
                <td>{{ breakdown_symptom.id }}</td>
                <td>{{ breakdown_symptom.name }}</td>
                <td>
                    <a href="{{ path('breakdown_symptom_show', {'id': breakdown_symptom.id}) }}">show</a>
                    <a href="{{ path('breakdown_symptom_edit', {'id': breakdown_symptom.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('breakdown_symptom_new') }}">Create new</a>
{% endblock %}
