{% extends 'base.html.twig' %}

{% block title %}EquipmentSoftware{% endblock %}

{% block body %}
    <h1>EquipmentSoftware</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipment_software.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ equipment_software.date ? equipment_software.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('equipment_software_index') }}">back to list</a>

    <a href="{{ path('equipment_software_edit', {'id': equipment_software.id}) }}">edit</a>

    {{ include('equipment_software/_delete_form.html.twig') }}
{% endblock %}
