{% extends 'admin/index.html.twig' %}

{% block title %}Modifier un dépot{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">

        <div class="col-md-4">

            <h1>Modifier un dépot</h1>

            {{ include('admin/site/_form.html.twig', {'button_label': 'Modifier'}) }}
            <div class="d-flex justify-content-between mt-3">

                <a class="btn btn-info" href="{{ path('site_index') }}">Retour</a>

                {{ include('admin/site/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
