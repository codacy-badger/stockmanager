<form method="post" action="{{ path('site_delete', {'id': site.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer le dépot?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete-site') }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
