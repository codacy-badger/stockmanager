{% extends 'admin/index.html.twig' %}

{% block body %}

    <div class="col-md-6">
        <h3>Ajout d'une localisation</h3>
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button class="btn btn-primary">Ajouter</button>
        {{ form_end(form) }}
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>

        $('#location_edit_form_equipment').autocompleter({
            url_list: '/member/equipment_search',
            url_get: '/member/equipment_get'
        });
    </script>
{% endblock %}

