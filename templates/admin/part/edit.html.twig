{% extends 'admin/index.html.twig' %}

{% block title %}Modifier une pièce{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">

        <div class="col-md-4">
            <h1>Modifier une pièce</h1>

            {{ include('admin/part/_form.html.twig', {'button_label': 'Modifier'}) }}
            <div class="d-flex mt-1 justify-content-between">
                <a class="btn btn-info" href="{{ path('part_index') }}"><i class="fas fa-undo-alt"></i> Retour à la
                    liste</a>

                {{ include('admin/part/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
