{% extends 'admin/index.html.twig' %}

{% block title %}Transportation{% endblock %}

{% block body %}
    <h1>Transportation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transportation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ transportation.name }}</td>
            </tr>
            <tr>
                <th>TradeName</th>
                <td>{{ transportation.tradeName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('transportation_index') }}">back to list</a>

    <a href="{{ path('transportation_edit', {'id': transportation.id}) }}">edit</a>

    {{ include('admin/transportation/_delete_form.html.twig') }}
{% endblock %}
