{% extends 'admin/index.html.twig' %}

{% block title %}Authorization{% endblock %}

{% block body %}
    <h1>Authorization</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ authorization.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ authorization.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('authorization_index') }}">back to list</a>

    <a href="{{ path('authorization_edit', {'id': authorization.id}) }}">edit</a>

    {{ include('admin/authorization/_delete_form.html.twig') }}
{% endblock %}
