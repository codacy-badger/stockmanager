{% extends 'base.html.twig' %}

{% block title %}Repair{% endblock %}

{% block body %}
    <h1>Repair</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ repair.id }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ repair.startDate ? repair.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndDate</th>
                <td>{{ repair.endDate ? repair.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ repair.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('repair_index') }}">back to list</a>

    <a href="{{ path('repair_edit', {'id': repair.id}) }}">edit</a>

    {{ include('admin/repair/_delete_form.html.twig') }}
{% endblock %}
