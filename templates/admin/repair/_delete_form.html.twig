<form method="post" action="{{ path('repair_delete', {'id': repair.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette réparation?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete-repair') }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
