{% extends 'admin/index.html.twig' %}

{% block title %}EquipmentSoftware index{% endblock %}

{% block body %}
    <h1>EquipmentSoftware index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for equipment_software in equipment_softwares %}
            <tr>
                <td>{{ equipment_software.id }}</td>
                <td>{{ equipment_software.date ? equipment_software.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('equipment_software_show', {'id': equipment_software.id}) }}">show</a>
                    <a href="{{ path('equipment_software_edit', {'id': equipment_software.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('equipment_software_new') }}">Create new</a>
{% endblock %}
