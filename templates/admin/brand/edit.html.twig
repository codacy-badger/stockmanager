{% extends 'admin/index.html.twig' %}

{% block title %}Modifier modèle{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">

        <div class="col-md-4">

            <h1>Modifier modèle </h1>

            {{ include('admin/brand/_form.html.twig', {'button_label': 'Modifier'}) }}

            <div class="d-flex justify-content-between mt-1">

                <a class="btn btn-info" href="{{ path('brand_index') }}">Retour à la liste</a>

                {{ include('admin/brand/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
