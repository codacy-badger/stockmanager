{% extends 'admin/index.html.twig' %}

{% block title %}Symptomes{% endblock %}

{% block body %}
    <h1>Symptomes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ symptom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ symptom.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('symptom_index') }}">back to list</a>

    <a href="{{ path('symptom_edit', {'id': symptom.id}) }}">edit</a>

    {{ include('admin/symptom/_delete_form.html.twig') }}
{% endblock %}
