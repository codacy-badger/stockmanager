<form method="post" action="{{ path('part-group_delete', {'id': partGroup.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce groupe de pièce?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete-partGroup') }}">
    <button class="btn btn-danger"><i class="fas fa-trash-alt"></i> Supprimer</button>
</form>
