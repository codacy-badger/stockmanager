{% extends 'admin/index.html.twig' %}

{% block title %}BreakdownSymptom{% endblock %}

{% block body %}
    <h1>BreakdownSymptom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ breakdown_symptom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ breakdown_symptom.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('breakdown_symptom_index') }}">back to list</a>

    <a href="{{ path('breakdown_symptom_edit', {'id': breakdown_symptom.id}) }}">edit</a>

    {{ include('breakdown_symptom/_delete_form.html.twig') }}
{% endblock %}
