<form method="post" action="{{ path('issue_symptom_delete', {'id': issue_symptom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ issue_symptom.id) }}">
    <button class="btn">Delete</button>
</form>
